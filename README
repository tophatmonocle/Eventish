Required modules:
express
mongodb
underscore

model:
{
	datetime: new Date(),
	tags: ["some", "list", "of", "tags"],
	data: { 
		key1: "val1",
		key2: "val2"
	}
}


curl:
curl -i -H "Content-Type: application/json" -X POST -d "tags":"[a,b,c]" http://localhost:3100
